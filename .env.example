# Chuỗi kết nối Postgres production. Bỏ trống = dùng PGlite local (chỉ cho dev).
DATABASE_URL=postgres://roomio:matkhau@localhost:5432/roomio
# Khóa ký cookie phiên — BẮT BUỘC đổi ở production: openssl rand -base64 48
SESSION_SECRET=doi-chuoi-nay-o-production
# Domain công khai (bắt buộc cho upload form khi chạy adapter-node)
ORIGIN=https://roomio.example.com
# Cổng API
PORT=3000
# payOS Merchant API credentials (my.payos.vn -> Kênh thanh toán)
PAYOS_CLIENT_ID=
PAYOS_API_KEY=
PAYOS_CHECKSUM_KEY=
# URL frontend công khai để payOS redirect sau thanh toán/hủy
PUBLIC_APP_ORIGIN=https://roomio.example.com

# Cloudflare R2 (S3-compatible) để cấp pre-signed URL cho web/TMA upload ảnh trực tiếp
R2_ACCOUNT_ID=
R2_ACCESS_KEY_ID=
R2_SECRET_ACCESS_KEY=
R2_BUCKET=roomio-uploads
# Domain public/read-only trỏ vào bucket, ví dụ https://assets.roomio.example.com
R2_PUBLIC_BASE_URL=
R2_PRESIGN_EXPIRES_SECONDS=300
R2_UPLOAD_MAX_BYTES=5242880
