# Chuỗi kết nối Postgres production. Bỏ trống = dùng PGlite local (chỉ cho dev).
DATABASE_URL=postgres://roomio:matkhau@localhost:5432/roomio
# Khóa ký cookie phiên — BẮT BUỘC đổi ở production: openssl rand -base64 48
SESSION_SECRET=doi-chuoi-nay-o-production
# Domain công khai (bắt buộc cho upload form khi chạy adapter-node)
ORIGIN=https://roomio.example.com
# Cổng API
PORT=3000
# (Tùy chọn) API key xác thực webhook SePay
SEPAY_API_KEY=
